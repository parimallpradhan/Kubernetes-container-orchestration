1. What is Kubernetes?
2. Why was Kubernetes needed?
3. What problem does it solve?
4. What is the difference between Docker and Kubernetes?
5. What is container orchestration?
6. What can Kubernetes do?
7. What is a Kubernetes cluster?
8. What are Node, Control Plane and Worker Node?
9. Where is Kubernetes used in real projects?

---

# 1. Real-world scenario

> "Suppose we have a Java application running in Docker."

Initially:

```text
Developer
    |
    v
Docker Image
    |
    v
Docker Container
    |
    v
Application
```

Ask them:

**"What happens if I have 1 container?"**

Easy.

```text
Server
 |
 └── Container
      └── Java Application
```

Now ask:

**"What if I have 10 containers?"**

```text
Server
 |
 +── Container 1
 +── Container 2
 +── Container 3
 +── Container 4
 +── ...
 +── Container 10
```


> "Who will monitor these containers?"

> "Who will restart a failed container?"

> "Who will distribute traffic?"

> "Who will create additional containers when traffic increases?"

> "Who will deploy a new version?"

This creates the **need for Kubernetes**.

---

# 2. Problem before Kubernetes

Diagram:

```text
             Docker Host
                  |
      +-----------+-----------+
      |           |           |
      v           v           v
   Container   Container   Container
      |           |           |
      v           v           v
    App A       App B       App C
```

Now imagine:

```text
100 Containers
10 Servers
5 Applications
Thousands of Users
```

Managing this manually becomes difficult.

Problems:

* Container failure
* Server failure
* Scaling
* Load balancing
* Deployment
* Rollback
* Networking
* Service discovery
* High availability
* Resource management

Then introduce:

# Kubernetes

> **Kubernetes is a container orchestration platform used to deploy, manage, scale, and maintain containerized applications.**

The important word is:

### **ORCHESTRATION**

---

# 3. What is Container Orchestration?

### 🎼 Orchestra analogy

In an orchestra, you have:

```text
             Conductor
                 |
     +-----------+-----------+
     |           |           |
   Violin      Piano       Drums
```

The conductor coordinates everyone.

Similarly:

```text
             Kubernetes
                 |
     +-----------+-----------+
     |           |           |
 Container    Container    Container
```

Kubernetes coordinates containers.

> Docker helps us **run containers**.

> Kubernetes helps us **manage containers at scale**.

---

# 4. Docker vs Kubernetes

This is one of the most important concepts for beginners.

| Docker                        | Kubernetes                       |
| ----------------------------- | -------------------------------- |
| Container platform            | Container orchestration platform |
| Builds images                 | Deploys/manages containers       |
| Runs containers               | Manages many containers          |
| `docker run`                  | Kubernetes Deployment/Pod        |
| Manual scaling                | Automated scaling                |
| Basic networking              | Advanced service networking      |
| Container-focused             | Cluster-focused                  |
| Good for individual workloads | Good for large-scale workloads   |

Example:

```text
Docker:

Server
 ├── Container
 ├── Container
 └── Container
```

Kubernetes:

```text
Kubernetes Cluster
        |
   +----+----+
   |         |
Node       Node
 |           |
Pods        Pods
```

---

# 5. Why not just use Docker?


> "If Docker can run containers, why do we need Kubernetes?"

Scenario:

```text
E-Commerce Application
        |
        +--- Frontend
        +--- Backend
        +--- Payment
        +--- Order
        +--- Database
```

Suppose traffic suddenly increases:

```text
Normal traffic
     |
     v
3 containers
```

During sale:

```text
Huge traffic
     |
     v
30 containers required
```

Kubernetes can help automate scaling.

Similarly:

```text
Container crashes
       |
       v
Kubernetes detects it
       |
       v
Creates replacement
```

That's called **self-healing**.

---

# 6.Kubernetes capabilities

### 1. Container Deployment

Deploy applications into containers.

### 2. Scaling

```text
3 Pods
   |
   | traffic increases
   v
10 Pods
```

### 3. Self-Healing

```text
Pod 1
Pod 2
Pod 3
  X
Pod 3 crashes

Kubernetes
     |
     v
New Pod created
```

### 4. Load Balancing

```text
              Users
                |
                v
           Kubernetes
                |
       +--------+--------+
       |        |        |
      Pod      Pod      Pod
```

Traffic can be distributed across application instances.

### 5. Rolling Updates

Explain:

```text
Version 1
   |
   v
Version 2
```

Kubernetes can gradually replace old application instances with new ones.

### 6. Rollback

If the new version has a problem:

```text
v2
 |
 | Problem
 v
Rollback
 |
 v
v1
```

### 7. Service Discovery

Applications need to find each other.

```text
Order Service
      |
      v
Payment Service
```

Kubernetes provides mechanisms for services to communicate without manually tracking changing container IP addresses.

---

# 7. Kubernetes Cluster

Now move from **why** to **architecture**.

Draw:

```text
              Kubernetes Cluster
                     |
          +----------+----------+
          |                     |
     Control Plane          Worker Node
```

### Control Plane

> **"Brain of Kubernetes"**

It makes decisions and manages the cluster.

### Worker Node

Think:

> **"Where applications actually run."**

Then:

```text
              Kubernetes Cluster
                     |
          +----------+----------+
          |                     |
   Control Plane            Worker Node
                                |
                         +------+------+
                         |             |
                        Pod           Pod
```


# 8. Pod 

> "Kubernetes does not normally deploy a container directly. Kubernetes runs containers inside a unit called a **Pod**."

Diagram:

```text
Kubernetes
     |
    Pod
     |
Container
     |
Application
```


**Kubernetes → Pod → Container → Application**

---

# 9. Real production architecture

This is where your DevOps experience can make the class interesting.


```text
                    Users
                      |
                      v
                   Ingress
                      |
              +-------+-------+
              |               |
              v               v
         Frontend          Backend
           Pods              Pods
                              |
                         +----+----+
                         |         |
                         v         v
                      Payment    Order
                       Pods       Pods
                             
                              |
                              v
                           Database
```

> "This is the type of architecture you will eventually learn to deploy."

---

# 10. Kubernetes evolution


Teach:

```text
Traditional Deployment
        |
        v
Virtual Machines
        |
        v
Docker Containers
        |
        v
Container Orchestration
        |
        v
Kubernetes
```

Explain the progression.

### Traditional

```text
Application
   |
Server
```

Problem: resource wastage and difficult deployment.

### Virtual Machines

```text
Physical Server
   |
 +-----+-----+
 | VM1 | VM2 |
 +-----+-----+
```

Better isolation, but VMs are heavier.

### Containers

```text
Docker
 |
 +-- Container
 +-- Container
 +-- Container
```

Lightweight and portable.

### Kubernetes

```text
Kubernetes
 |
 +-- Node
 |    +-- Pod
 |    +-- Pod
 |
 +-- Node
      +-- Pod
      +-- Pod
```

Now we can manage containers at scale.

---

# 11. Why Kubernetes became popular?



* Microservices
* Cloud-native applications
* Scalability
* High availability
* Automated deployments
* Self-healing
* Rolling updates
* Large number of containers
* Multi-node environments


---

# 12. Who uses Kubernetes?


* Google
* Microsoft
* Amazon
* Netflix
* Spotify
* Airbnb
* Many enterprises and startups


> "Kubernetes is especially useful when applications have many services, many containers, and requirements for automation, scaling and availability."

---

# 13. Model



```text
                KUBERNETES
                     |
             Container Orchestration
                     |
        +------------+------------+
        |            |            |
      Deploy       Scale       Manage
        |            |            |
     Containers   Containers   Containers
                     |
                  Cluster
                     |
          +----------+----------+
          |                     |
    Control Plane          Worker Nodes
                                |
                               Pods
                                |
                           Containers
                                |
                           Applications
```

## 🕐 Summay

|   Time | Topic                                        |
| -----: | -------------------------------------------- |
| 10 min | What happens when we run a Docker container? |
| 10 min | Problems with managing many containers       |
| 10 min | What is container orchestration?             |
| 15 min | What is Kubernetes?                          |
| 10 min | Docker vs Kubernetes                         |
| 10 min | Kubernetes features                          |
| 10 min | Kubernetes cluster                           |
|  5 min | Control Plane vs Worker Node                 |
|  5 min | Pod introduction                             |
|  5 min | Real-world architecture                      |



* YAML syntax
* `kubectl`
* Pod YAML
* Deployment YAML
* ReplicaSet
* Services in detail
* ConfigMap
* Secrets
* Ingress
* Volumes
* StatefulSets
* DaemonSets
* Helm
* RBAC


